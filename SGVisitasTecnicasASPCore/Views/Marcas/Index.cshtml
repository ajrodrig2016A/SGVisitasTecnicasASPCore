@model IEnumerable<SGVisitasTecnicasASPCore.Models.marcas>

@{
    ViewData["Title"] = "Gestión de Marcas";
    Layout = "~/Views/Shared/_Layout.cshtml";

    SortModel sortModel = (SortModel)ViewData["sortModel"];

    PagerModel pager = ViewBag.Pager;
}

<hr />
<h4 class="text-center text-primary">Gestión de Marcas</h4>
<hr />
<div class="container">
    <p>
        <a class="btn btn-primary" asp-action="Create">Nueva Marca</a>
    </p>
    <div class="row">
        <div class="col-12">
            <partial name="_topBar" model="@pager" />
        </div>
    </div>
    <section id="no-more-tables" style="padding-top: 0;">
        <table class="table-bordered table-striped table-condensed">
            <thead class="text-center">
                <tr>
                    <th>
                        <i class="@sortModel.GetColumn("Nombre").SortIcon" arial-hidden="true"></i>

                        <a asp-action="Index" asp-route-sortExpression="@sortModel.GetColumn("Nombre").SortExpression">
                            Nombre
                        </a>
                    </th>
                    <th>
                        <i class="@sortModel.GetColumn("Descripción").SortIcon" arial-hidden="true"></i>

                        <a asp-action="Index" asp-route-sortExpression="@sortModel.GetColumn("Descripción").SortExpression">
                            Descripción
                        </a>
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td data-title="@Html.DisplayNameFor(model => model.nombre)">
                            @Html.DisplayFor(modelItem => item.nombre)
                        </td>
                        <td data-title="@Html.DisplayNameFor(model => model.descripcion)">
                            @Html.DisplayFor(modelItem => item.descripcion)
                        </td>
                        <td>
                            <div class="btn-group">
                                <a asp-action="Edit" asp-route-id="@item.id_marca">
                                    <span class="btn btn-sm btn-outline-primary">
                                        <i class="fa fa-edit" aria-hidden="true"></i>
                                    </span>
                                </a>

                                <a asp-action="Details" asp-route-id="@item.id_marca">
                                    <span class="btn btn-sm btn-outline-dark">
                                        <i class="fa fa-eye" aria-hidden="true"></i>
                                    </span>
                                </a>

                                <a asp-action="Delete" asp-route-id="@item.id_marca">
                                    <span class="btn btn-sm btn-outline-danger">
                                        <i class="fa fa-trash" aria-hidden="true"></i>
                                    </span>
                                </a>

                            </div>
                            @*@Html.ActionLink("Editar", "Edit", new { id = item.id_marca }, new { @class = "btn btn-warning" }) |
                                @Html.ActionLink("Detalles", "Details", new { id = item.id_marca }, new { @class = "btn btn-info" }) |
                                @Html.ActionLink("Eliminar", "Delete", new { id = item.id_marca }, new { @class = "btn btn-danger" })*@
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </section>
    <div class="row">
        <div class="col-12">
            <partial name="_bottomBar" model="@pager" />
        </div>
    </div>
</div>

@section Scripts {
    <script type="text/javascript">
        function openSuccessModal(strMessage) {
            var myDiv = document.getElementById("myModalSuccessAlertBody");
            myDiv.innerHTML = strMessage;
            $('#myModalSuccess').modal('show');

        }

        $(document).ready(function () {
            var msg = "@TempData["SuccessMessage"]";
            if (msg) {
                openSuccessModal(msg);

            }

        });

    </script>
}